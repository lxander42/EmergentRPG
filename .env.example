# Anthropic API key used by the narrate / npc-dialogue route handlers.
# Get one from https://console.anthropic.com/ and add it to Vercel env vars
# (Production + Preview) under Project Settings → Environment Variables.
ANTHROPIC_API_KEY=
